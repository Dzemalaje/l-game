node_modules/
dist/

# esbuild output for the SpacetimeDB play page; rebuilt by `npm run stdb:play`
spacetimedb/play/bundle.js
spacetimedb/play/bundle.js.map
tools/screenshots/
.env
.env.*
!.env.example
!.env.deploy.example

# Expo prebuild output; regenerate with `npx expo prebuild`
android/
ios/
.expo/

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

# expo-env.d.ts is deliberately NOT ignored. tsconfig includes it, and it is the only thing that
# pulls in expo/types - which declares `*.css`. Ignored, a fresh clone fails `npm run typecheck`
# on the two global stylesheet imports before anything has a chance to regenerate it. The sibling
# uniwind-env.d.ts is generated and committed for the same reason.
# @end expo-cli
